---
import { Icon } from 'astro-icon/components';

interface Props {
  class?: string;
}

const { class: className = 'size-5' } = Astro.props;
---

<Icon name="lucide:heart" class={className} aria-hidden="true" />
